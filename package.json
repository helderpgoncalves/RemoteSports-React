{
  "name": "remotesports",
  "version": "0.1.0",
  "private": true,
  "main": "server.js",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^2.0.6",
    "socket.io": "^2.3.0",
    "socket.io-client": "^3.0.4",
    "xss": "^1.0.8"
  },
  "scripts": {
    "start": "nodemon server.js",
    "heroku-postbuild": "npm start && cd client && npm install --only=dev && npm install && npm run build"
  }
}
