export interface scrollLockOptions {
    container: HTMLElement;
}
declare let uuid: number;
export default class ScrollLocker {
    lockTarget: typeof uuid;
    options: scrollLockOptions;
    constructor(options?: scrollLockOptions);
    lock: () => void;
    unLock: () => void;
}
export {};
